# desk backend
Backend logic for shared desk reservation
NOTE : this app is configured to use https redirection, so running the https launch setting is recommended,
if there is a pop-up for dev certficate click 'yes'.
